# Devpost update package

Prepared on 2026-09-04 after comparing the submitted public page with the
current release candidate. Do not edit the live submission until the replacement
video has been rendered, uploaded, processed in HD, and verified.

## What is stale on Devpost

- The embedded video is the historical 94-second Sofia-to-Daniel demo.
- The story describes five historical WebMCP tools and the old flow.
- The gallery shows the historical Sofia/Daniel run and compensation screens.
- The testing instructions refer to the five-tool implementation.

The live demo URL and GitHub repository URL are already correct.

## Overview fields

Project name:

> Teachback

Tagline:

> Turn one handled case into a reusable WebMCP playbook—drafted by an agent, bounded and applied by people.

Cover image:

- `public/devpost-thumbnail.png`
- 1200 × 800 pixels (3:2)

## Project story

Replace the current story with the complete copy in [`ENTRY.md`](ENTRY.md),
from **What Teachback does** through **Prototype scope**. Use its **Built with**
list and current links as well.

## Video

Replace the historical YouTube URL with
<https://www.youtube.com/watch?v=dqei8azzFZo>. The title, description, tags,
and settings are in [`YOUTUBE.md`](YOUTUBE.md).

## Gallery order and captions

1. `screenshots/01-recorded-evidence.png`
   **Recorded work becomes structured evidence.** Teachback preserves the four
   saved operations, exact diffs, and evidence IDs instead of recording clicks.
2. `screenshots/02-agent-draft.png`
   **The agent drafts; the website keeps control.** A WebMCP-capable agent turns
   the record into operations, reusable fields, and wording.
3. `screenshots/03-human-boundary.png`
   **A person sets the reusable boundary.** The operator reviews the proposed
   wording and tightens the allowed arrival time before publishing.
4. `screenshots/04-prepared-not-applied.png`
   **Preparation is not execution.** The website returns exact proposed changes
   through WebMCP while keeping the reservation unchanged.
5. `screenshots/05-human-approved-commit.png`
   **Human approval gates the commit.** Only the exact approved run can be
   applied, with expiry, digest, version, and replay checks.
6. `screenshots/06-website-refusal.png`
   **The website enforces the boundary.** A compensation request is refused and
   returned to a person instead of being forced through by the agent.

## Additional information

- Replace the current judge instructions with [`TESTING.md`](TESTING.md).
- State that the current page registers seven typed WebMCP tools, using the
  exact tool names listed in [`ENTRY.md`](ENTRY.md).
- Keep submitter type **Individual**, country **Japan**, app status **New**, and
  the current live/source URLs.
- Describe HeyGen as the narration source and HyperFrames as the editor. Do not
  claim that the current Devpost cover was generated by an AI image tool.

## Final live verification

After saving all pages:

1. Reload each Devpost edit step and verify the saved values.
2. Confirm the public project shows the new video, cover, story, and gallery.
3. Confirm the project status is **Submitted**, not draft.
4. Open the public entry signed out and archive its final URL and visible receipt.
